You are helping with a minimal graph coloring task.
- Input: graph dict with "edges" as list of (u, v) pairs.
- Output: dict mapping node -> color_id (int).
- Constraint: adjacent nodes must have different colors.
- Objective: use as few colors as possible; a simple greedy or DSATUR heuristic works.

Write Python code that defines:

def color_graph(graph):
    # return {node: color}

Use only standard library. Avoid external dependencies and randomness for reproducibility.
